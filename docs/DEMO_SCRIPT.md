# DEMO_SCRIPT.md — 提出用デモ動画 / 最終ピッチ スクリプト

> Findy DevOps × AI Agent Hackathon 2026 の **作品提出用デモ動画** (3 分以内) と **最終ピッチ (8/19) 用プレゼン** のシナリオ・セリフ・カット割り。
>
> **v1.0 (2026-07-02)**: 実装済みの実体に合わせて全面改訂。Veo 言及を全削除 (未使用のため。PROJECT.md §4 参照)。ヒーローを Watcher (街の見張り番エージェント) に確定。
> 撮影は 7/3-7/6、ProtoPedia 登録 7/7-7/8、Google Form 提出 7/9 (締切 7/10 23:59 の 1 日前)。

---

## 0. 制作方針

### 0.1 動画の目的（2種類）

| 動画 | 目的 | 長さ | 視聴者 |
|---|---|---|---|
| **A. 提出用デモ動画** | Proto Pedia / Google Form 提出。一次・二次審査で見られる | 3 分以内 | 審査員（運営事務局・外部有識者） |
| **B. 最終ピッチ** | 最終ピッチ会場 (8/19) で生披露 | 5-7 分 | 審査員 + 観客 (Google渋谷) |

両者で同じ素材を活用し、A は短縮版、B はライブ操作含む拡張版にする。

### 0.2 編集原則

1. **冒頭3秒で興味を掴む**（痛みのある問い）
2. **15秒で「これは何か」を理解させる**（一言コンセプト）
3. **中盤は体験デモ**（実 UI の操作のみ。モック・想像図は使わない）
4. **技術は「自律性が見える画面」で語る**（アーキ図の静止画は最小限）
5. **主張はすべて実装済みの事実**（未実装・未使用技術への言及は禁止。審査基準⑤「主張と実体の整合」対策）
6. 字幕を必ず焼き込む（審査員の多くが音声OFFで見る想定）

### 0.3 ツール

- 画面録画: OBS Studio / ブラウザ録画
- 編集: DaVinci Resolve (無料) or CapCut
- ナレーション: 自分の声 (Yuji)、BGM: YouTube オーディオライブラリ
- 実演 URL: `https://citify-web--citify-dev.asia-east1.hosted.app/`

### 0.4 事前セットアップ（撮影前日までに必ず）

- [ ] **データ鮮度**: Cloud Scheduler 5 job を resume（`./toggle-schedulers.sh resume` 相当）し、必要なら `publish-all` で議題を追加投入
- [ ] **フィード用ペルソナ**: `/municipalities` で **岡山市(33100)・札幌市(01100)・松山市(38201)** を登録（データ最多の 3 都市。「あなたの街」タブが確実に埋まる）
- [ ] **Watcher 用**: トップのプリセットペルソナ（例: 子育て世帯）を 1 つ選び、`/agent` で一度分析を実行してキャッシュを作っておく（本番録画でのレイテンシ対策。録画では再実行してトレースを見せる）
- [ ] ブラウザはシークレットウィンドウ + 日本語 UI + 375px 幅（スマホ枠）で統一

---

## 1. デモ動画 (3 分版) — 提出用

### 1.1 全体構成

| 時間 | シーン | 画面 | 伝えること |
|---|---|---|---|
| 00:00-00:15 | ① 課題提起 | 黒背景字幕 → 自治体HPスクショ | 「議会の議論、誰も読んでいない」 |
| 00:15-01:10 | ② コア体験 | `/` → `/feed` → `/feed/[id]` | 翻訳・パーソナル化・RAG・原典 |
| 01:10-02:00 | ③ Watcher（ヒーロー） | `/agent` → `/plan` | エージェントの自律性（計画→並列実行→自己検証） |
| 02:00-02:20 | ④ 比較 + コンシェルジュ | `/compare` → `/concierge` | 自治体HPには構造的に不可能な体験 |
| 02:20-02:50 | ⑤ アーキテクチャ + DevOps | アーキ図 + CI画面 | 13エージェント / GCP フルスタック / IaC |
| 02:50-03:00 | ⑥ クロージング | ロゴ | 社会的意義 |

> 尺配分メモ (レビュー反映): ⑤のナレは約 200 字 = 読み上げ 30 秒強のため 30 秒を確保。②③を各 5 秒圧縮した。

### 1.2 詳細スクリプト

#### 🎬 シーン① 課題提起 (00:00-00:15)

**画面**: 黒背景 → 実際の自治体HP・議事録ページのスクショ（文字がびっしり）

**字幕/ナレ**:
> あなたの街の議会で、先週なにが議論されたか知っていますか？
>
> 家賃補助も、子育て支援も、防災も。全部そこで決まっているのに——誰も読まない。

---

#### 🎬 シーン② コア体験 (00:15-01:10)

**操作 1 (00:15-00:30)** — トップ `/`:
- プリセットペルソナ（例:「子育て世帯・東京から地方へ」）をタップ → フィードへ

**ナレ**:
> Citify は、自治体の「今」をあなた向けに翻訳して届ける AI プロダクトです。
> 年代と関心を選ぶだけ。

**操作 2 (00:30-00:48)** — `/feed`:
- 「あなたの街」タブ: 岡山市・札幌市・松山市の議題カードが並ぶ
- カードには: 平易化タイトル / 3行サマリ / 自治体名 / 関心タグ / Imagen 生成サムネ（「✨AI生成画像」ラベル）
- 2〜3 枚縦スクロール

**ナレ**:
> 議事録とプレスリリースを AI が読み、役所言葉を「あなたの年代の言葉」に翻訳。
> 関心とのマッチ度で採点された議題が、For You フィードに流れてきます。

**操作 3 (00:48-01:10)** — 議題詳細 `/feed/[speech_id]`:
- 平易タイトルと**正式名の併記**を見せる
- 4軸スコア（トピック/年代/地理/緊急度）の横棒
- **国会の関連議題 3 件**（Vertex AI RAG Engine、国会議事録コーパス 1,428 件）
- **原典リンク**を明示 → 「気になる」タップ → 世代別リアクション集計

**ナレ**:
> 深掘りしたければ、RAG が**国会議事録**から関連する論戦を引いてきます。
> 原典には必ず 1 タップで飛べる。要約だけで完結させない設計です。

> ⚠️ 正確性メモ: RAG コーパスは現状**国会議事録のみ** (1,428 件)。「地方議題の関連議事録を検索」とは言わないこと。

---

#### 🎬 シーン③ Watcher — ヒーロー機能 (01:10-02:00)

**操作 1 (01:10-01:35)** — `/agent` で「分析を実行」:
- **自律実行トレース**が流れる画面をそのまま見せる:
  プランナーが調査計画を立てる → 複数の専門ツール（街の統計比較・議題検索・人口推移・トレンド）を**並列で呼び出す** → 収集結果を自己検証してから結論を出す

**ナレ**:
> Citify の中心は「街の見張り番」Watcher エージェント。
> あなたのペルソナを読み、**自分で調査計画を立て、必要なツールを選んで並列実行し、
> 結論を出す前に自己検証する**。ADK で実装した自律エージェントです。

**操作 2 (01:35-02:00)** — 分析結果 → `/plan`:
- 街ごとの評価カード + レーダーチャート + 総合 verdict
- 続けて `/plan`: verdict を行動に落としたアクションプラン

**ナレ**:
> 結論は「どの街が、あなたの何に合うか」。根拠の議題にはその場で飛べます。
> さらに、結論をそのままアクションプランに変換。「知る」で終わらせません。

---

#### 🎬 シーン④ 比較 + コンシェルジュ (02:00-02:20)

**02:00-02:10** — `/compare`:
- 2〜3 自治体 × テーマの横並び比較 + AI の中立観察
- ⚠️ **テーマは「子育て」を使う** (BQ 実測 2026-07-02: 子育て=岡山6/札幌5/松山2 で両側に議題あり。教育・雇用も可。**住居は岡山側が空**になるので使わない)

**02:10-02:20** — `/concierge`:
- 自然言語で自己紹介 → 候補の街カードが返るチャットを数秒

**ナレ**:
> 街同士の比較も、対話での街探しも。
> コンシェルジュは翻訳・影響度エージェントを**サブエージェント**として従える ADK の親子階層です。

---

#### 🎬 シーン⑤ アーキテクチャ + DevOps (02:20-02:50)

**画面**: `docs/assets/architecture.svg`（ズーム/パン）→ GitHub Actions green → Cloud Build → Terraform plan 出力

**ナレ** (約 30 秒):
> 裏側は 13 の AI エージェント。翻訳・採点・配信の Pub/Sub パイプライン、
> ADK の自律エージェント、スクレイパー診断の運用エージェント。
> 全国 **830 の自治体・議会**、**3,700 件超の議題**を処理しています。
>
> インフラは全て Terraform。push だけで本番までデプロイされます。

**画面の隅に常時表示する技術ロゴ/テキスト**:
`Cloud Run / ADK / Gemini 2.5 / Vertex AI RAG Engine / Imagen 3 / BigQuery / Pub/Sub / Firestore / Firebase App Hosting / Terraform`
（**Veo は表示しない** — 未使用）

---

#### 🎬 シーン⑥ クロージング (02:50-03:00)

**ナレ**:
> 自治体は、若者の生活そのものです。でも、若者は自治体を知らない。
> Citify は、その距離を 60 秒で縮めます。

**字幕**:
> 🏛️ Citify — 自分の街、自分の世代の話を、60秒で。

---

## 2. 最終ピッチ (5-7 分版) — 8/19 用

3 分デモに以下を追加:

| 追加 | 内容 |
|---|---|
| 前 30 秒 | 自己紹介 + 当事者性（上京 5 年、区議会を一度も読んだことがなかった） |
| 中 60 秒 | ライブ操作（Watcher の分析実行を目の前で。キャッシュ済ペルソナをバックアップに） |
| 後 90 秒 | 技術深掘り 1 本: **Watcher の自律性設計**（下記） |
| 終 45 秒 | 展望 + Q&A 準備（Watcher 非同期実行の遅延に備え 15 秒バッファ） |

### 技術深掘りスクリプト（Watcher の自律性）

> ここだけ 90 秒、技術の話をさせてください。
>
> Watcher は最初、完全自律型 — エージェントが毎ステップ次の行動を LLM で決める設計にしました。
> 動きはしたが、本番では逐次実行でレイテンシが破綻し、モデルの癖でツール選択も偏った。
>
> そこで「**計画は LLM、実行は並列**」に再設計しました。プランナーが最初に調査計画を立て、
> ツール呼び出しは並列で走らせ、結論の前に自己検証を挟む。
> カバレッジの床を保証するガードレールも入れています。
> 自律性は「全部 LLM に任せること」ではなく、**判断と実行の役割分担を設計すること** — これが 7 週間で得た結論です。

### 想定 Q&A

**Q1: 政治的中立性は？**
> 3 層で担保。①プロンプトで賛否表明を禁止 ②出力後の禁止語・政治家名・党名検出で再生成 or ルールベース代替 ③Imagen は `person_generation="dont_allow"` + AI 生成ラベル + SynthID。全議題に原典リンクを併記します。

**Q2: スクレイピングの適法性は？**
> robots.txt と利用規約を必ず尊重します。実例として、大手議事録システムの 1 つは robots.txt が議事録パスを全面 Disallow だったため、**対応済みコードごと Drop しました**。全文転載はせず要約 + 原典リンクに統一しています。

**Q3: LLM コストは？**
> 翻訳・採点は Gemini 2.5 Flash 中心、フィードは in-memory キャッシュ + Cache-Control、Cloud Run は scale-to-zero、バッチは Cloud Scheduler で 1 日 1 回。さらにコスト異常を検知する Cost Anomaly Hunter エージェントを自前で運用しています。

**Q4: ADK を使った理由は？**
> 自律ツールループ（Watcher）、サブエージェント親子階層（Concierge → 翻訳/影響度）、構造化アウトプットが標準化されているからです。一方、翻訳→採点→配信の定型パイプラインは ADK ではなく Pub/Sub + Cloud Run Jobs を選びました。**適材適所の使い分け**も設計判断として説明できます。

**Q5: 既存アプリとの違いは？**
> 一般の自治体ニュースアプリは「集約・配信」止まり。Citify は ①若者向け翻訳 ②年代×関心のパーソナル化 ③自治体比較 ④Watcher による自律分析、の 4 点で違います。特に③④は自治体 HP には構造的に提供できません。

**Q6: DevOps テーマへの貢献は？**
> Terraform で全リソース管理、GitHub Actions + Cloud Build の CI/CD、パスベース変更検知、Cloud Logging での観測。加えて**運用そのものをエージェント化**しています — スクレイパー失敗を診断・修正提案する Scraper Doctor、コスト異常検知の Cost Hunter。どちらも自動実行はせず人間レビュー前提の提案までです。

---

## 3. 撮影プラン（残り日程）

| 日 | 内容 |
|---|---|
| 7/3 (金) | §0.4 セットアップ + シーン②③ 画面録画 |
| 7/4 (土) | シーン④⑤ 録画 + アーキ図・CI 画面素材 |
| 7/5 (日) | ナレーション収録 + 編集 |
| 7/6 (月) | 字幕焼き込み・最終調整 → YouTube 限定公開 |
| 7/7 (火) | ProtoPedia 作品登録（`docs/submission/PROTOPEDIA.md` を転記） |
| 7/8 (水) | ProtoPedia 公開 + 内容最終確認 |
| 7/9 (木) | **Google Form 正式応募**（締切前日に完了させる） |
| 7/10 (金) | バッファ（提出確認メール保存） |

### 編集チェックリスト

- [ ] 字幕がすべてのナレに対応
- [ ] AI 生成画像に「AI 生成」ラベルが映っている
- [ ] 政治家の顔・名前が一切映っていない
- [ ] **Veo・未実装機能への言及ゼロ**（口頭・字幕・ロゴすべて）
- [ ] 3 分以内
- [ ] YouTube 限定公開 URL を ProtoPedia に登録後、提出時に公開

---

## 4. ProtoPedia 投稿

**→ `docs/submission/PROTOPEDIA.md` に完全版を用意**（タイトル/概要/ストーリー/開発素材/タグ）。転記するだけの状態にする。

---

## 改訂履歴

- 2026-07-02 v1.0 実体ベースで全面改訂（Veo 削除、Watcher ヒーロー化、実データ数値反映、日程を残り 8 日に更新）
- 2026-05-19 v0.1 初版作成
